<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.maniy.tot_application">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.SEND_SMS" />

    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LocationActivity"
            android:label="Location Share" />
        <activity
            android:name=".SMSActivity"
            android:label="SMS" />
        <activity android:name=".UserDetailsActivity"/>
        <activity android:name=".ChatActivity" android:label="Chats" />
        <activity android:name=".FoodOrderActivity" />
        <activity android:name=".FoodEnterActivity" />
        <activity android:name=".FoodConfirmActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".LastActivity" />

    </application>

</manifest>